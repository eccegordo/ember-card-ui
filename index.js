/* jshint node: true */
'use strict';

module.exports = {
  name: 'ember-card-ui',
  // allows scss processing
  // source ember cli sass readme
  included: function(app) {
    this._super.included(app);
  }
};
